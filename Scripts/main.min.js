$(document).ready(function () {
    $("#slidebartoggle").click(function () { $("#mobilenavtab").addClass("activetab") }); $("#slidebartoggle2").click(function () { $("#mobilenavtab").removeClass("activetab") }); $(window).scroll(function () {
        if ($(window).scrollTop() >= 100) { $("header").addClass("stickey") }
        else { $("header").removeClass("stickey") }
        if ($('body .stickey').length != 0) { $("body").addClass("stickey-active") }
        else { $("body").removeClass("stickey-active") }
    }); (function ($) {
        var multipleSupport = typeof $('<input/>')[0].multiple !== 'undefined', isIE = /msie/i.test(navigator.userAgent); $.fn.customFile = function () {
            return this.each(function () {
                var $file = $(this).addClass('custom-file-upload-hidden'), $wrap = $('<div class="file-upload-wrapper">'), $input = $('<input type="text" class="file-upload-input form-control" placeholder="Select File"/>'), $button = $('<button type="button" class="file-upload-button btn-main">Select</button>'), $label = $('<label class="file-upload-button btn-main" for="' + $file[0].id + '">Select</label>'); $file.css({ position: 'absolute', left: '-9999px' }); $wrap.insertAfter($file).append($file, $input, (isIE ? $label : $button)); $file.attr('tabIndex', -1); $button.attr('tabIndex', -1); $button.click(function () { $file.focus().click() }); $file.change(function () {
                    var files = [], fileArr, filename; if (multipleSupport) {
                        fileArr = $file[0].files; for (var i = 0, len = fileArr.length; i < len; i++) { files.push(fileArr[i].name) }
                        filename = files.join(', ')
                    } else { filename = $file.val().split('\\').pop() }
                    $input.val(filename).attr('title', filename).focus()
                }); $input.on({ blur: function () { $file.trigger('blur') }, keydown: function (e) { if (e.which === 13) { if (!isIE) { $file.trigger('click') } } else if (e.which === 8 || e.which === 46) { $file.replaceWith($file = $file.clone(!0)); $file.trigger('change'); $input.val('') } else if (e.which === 9) { return } else { return !1 } } })
            })
        }; if (!multipleSupport) { $(document).on('change', 'input.customfile', function () { var $this = $(this), uniqId = 'customfile_' + (new Date()).getTime(), $wrap = $this.parent(), $inputs = $wrap.siblings().find('.file-upload-input').filter(function () { return !this.value }), $file = $('<input type="file" id="' + uniqId + '" name="' + $this.attr('name') + '"/>'); setTimeout(function () { if ($this.val()) { if (!$inputs.length) { $wrap.after($file); $file.customFile() } } else { $inputs.parent().remove(); $wrap.appendTo($wrap.parent()); $wrap.find('input').focus() } }, 1) }) }
    }(jQuery)); $('input[type=file]').customFile()
}); $(document).ready(function () {
    $(".postcodetoggle").click(function () { $(".checkbox_mapbox").toggleClass("active") })
    $("#openrangbox").click(function () { $("#rangebox").addClass("active"); $(".togglefooterbtn").addClass("activebtn") })
    $(".maptoggle").click(function () { $(".mapbox_main").toggleClass("active") })
    if (typeof (Storage) !== "undefined") {
        $(document).ready(function ()
        { var showwelcomepopup = localStorage.getItem("showwelcomepopup"); if (showwelcomepopup == null || showwelcomepopup == undefined) { localStorage.setItem("showwelcomepopup", "1"); $('#welcomepopup').modal('toggle') } })
    }
    else {
        console.log('local storage not supported.')
        $('#welcomepopup').modal('toggle')
    }
}); $(document).ready(function () {
    var jazzimaging = $('.imgbox_agebox').height(); $('.imgbox_agebox').css('min-height', jazzimaging + 'px'); $(".checkbox_widthsub input[type='checkbox']").click(function () { if ($(this).is(":checked")) { $(this).closest('div').addClass('liked') } else { $(this).closest('div').removeClass('liked') } }); $(".yes_welcomepopup").click(function () { $('#welcomepopup').modal('hide'); $('#dashbord').modal('show'); $('.modal').css("margin-left", '-17px'); $('body').addClass("modal-open_signup") })
    $(".no_welcomepopup").click(function () {
        if (typeof (Storage) !== "undefined")
        { localStorage.setItem("showwelcomepopup", "1") }
    })
}); $(function () { $(".drp_date").hide(); $("input[name='foo']").click(function () { if ($("#setdate").is(":checked")) { $(".drp_date").show() } else { $(".drp_date").hide() } }) })